pipeline{
    agent any
    environment{
        staging_server="52.65.222.223"
    }
    stages{
        stage('Deploy to Remote'){
            steps{ 
                sh 'scp -r ${WORKSPACE}/* root@${staging_server}:/var/www/html/'
            }
        }
    }
} 
